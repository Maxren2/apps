# imap-ai

[imap-ai](https://github.com/Maxren2/imap-ai) is an AI email assistant that reads and sends mail over IMAP/SMTP
instead of provider REST APIs (Gmail API, Microsoft Graph), avoiding their per-minute quota throttling on
AI-heavy workloads like rule matching, chat, and bulk cleanup.
